//bomb_explode.sv
// bomb explosions

module bomb_explode(


input logic Reset, frame_clk, exist

output logic detonate
);

logic 


always_ff @(posedge Reset or posedge frame_clk) 
	begin	
		
	if (Reset)
		begin 
			